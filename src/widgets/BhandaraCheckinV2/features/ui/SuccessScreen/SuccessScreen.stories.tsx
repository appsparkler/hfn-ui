import type { Meta } from "@storybook/react";

import { EmailOrMobileCheckinScreen } from "./SuccessScreen";

const meta: Meta<typeof EmailOrMobileCheckinScreen> = {
  component: EmailOrMobileCheckinScreen,
};

export default meta;
